pipeline {
    agent {
        label 'maven'
    }

    stages {
        stage('Build') {
            steps {
                // One or more steps need to be included within the steps block.    
                echo 'Stage Build'
            }
        }

        stage('Test') {
            steps {
                // One or more steps need to be included within the steps block.
                echo 'Stage Test'
            }
  }

}

}